## 查 Retrieve

```sql
SELECT `name` FROM user_info;

SELECT `name`,age,'abc'  FROM user_info ORDER BY age ASC; --升序

SELECT `name`,age,'abc'  FROM user_info ORDER BY age DESC; --降序

SELECT DISTINCT location FROM employee; --去重
```

